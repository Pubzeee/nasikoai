version: '3.8'

services:
  app:
    build: .
    volumes:
      - .:/app
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    command: ["."]  # Default command analyzes the current directory
